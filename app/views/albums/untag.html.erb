<h4 class="page-header">Click a tag to remove</h4>

<ul class="remove-tag-buttons">
  <% @taggings.each do |tag| %>
    <li>
      <%= button_to "#" + tag.tag, 
        untag_band_url + "?tag_id=#{tag.id}", 
        method: :delete %>
      </li>
  <% end %>
</ul>

<ul class="page-links">
  <li><%= link_to "Back to #{@album.title}", album_url(@album), class: "button" %></li>
</ul>